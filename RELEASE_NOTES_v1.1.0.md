# Unity Underground 한국어 번역 v1.1.0

Ultima Underworld 1의 공식 영문 문자열을 기준으로 전체 번역을 다시 검토하고 패치 데이터에 반영한 완전 재번역판입니다.

## 주요 변경 사항

- 원작 영문 기준 7,092개 문자열 위치 전체 검토
- 149개 작업 배치와 재번역 묶음 1~33 전체 반영
- 블록별 번역 JSON 122개에서 5,644개 항목 변경
- `global_text_map.json` 전체 재생성 및 중복 원문 충돌 검증
- 인물별 말투, 고유명사, 퀘스트 단서, 아이템·주문·성소·덕목 용어 전면 정비
- 인터넷식 종결 표현, 오역, 직역투, 과도한 사극체와 존댓말 혼용 제거
- 플레이스홀더, 제어 명령, 줄바꿈과 앞뒤 공백 보존
- 한국어 어순이 필요한 동적 조립 문장 처리 보강

## 동적 문장과 중복 원문

- 런타임에서 여러 조각으로 조립되는 문장은 블록별 번역을 보존하고 플러그인에서 한국어 어순으로 재조립합니다.
- 같은 영문이 여러 화자에게 반복되면서 번역이 달라지는 항목은 전역 맵에서 가장 널리 쓰인 번역을 기준으로 통일했습니다.
- 블록별 JSON에는 위치별 검토 결과를 그대로 보존했습니다.
- 외부 원문 목록에 존재하지 않는 두 슬롯은 블록별 JSON에만 반영했습니다.

## 패키지

배포 ZIP에는 다음 파일만 포함됩니다.

- `BepInEx/plugins/UnityUndergroundKorean.dll`
- `translations` 폴더의 JSON 124개

## 설치

1. Unity Underground에 BepInEx 5.4.23.5를 설치합니다.
2. 배포 ZIP을 게임 폴더에 풉니다.
3. `BepInEx`와 `translations` 폴더를 덮어씁니다.
4. 게임을 실행합니다.

세이브 파일과 게임 원본 데이터는 수정하지 않습니다.

## 검증 결과

- 검토 위치: 7,092 / 7,092
- 번역 JSON: 124개 파싱 성공
- 미해결 원문 대응: 0개
- 토큰·제어 명령·구조 검증 문제: 0개
- 재적용 시 추가 변경 0개
- 배포 ZIP 무결성 검사 통과
