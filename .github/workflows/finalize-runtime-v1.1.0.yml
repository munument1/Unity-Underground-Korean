name: v1.1.0 Release Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is parked while the game-reference build is validated."
